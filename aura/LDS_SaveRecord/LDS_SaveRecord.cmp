<aura:component implements="flexipage:availableForAllPageTypes,lightning:actionOverride,force:hasSObjectName,flexipage:availableForRecordHome,force:hasRecordId" access="global" controller="LDS_Controller">
    
    <aura:attribute name="record" type="Object" />
    <aura:attribute name="recordError" type="String" />
    <aura:attribute name="simpleRecord" type="Object" />
    <aura:attribute name="fieldSet" type="List"  />
    <aura:attribute name="Labelname" type="string"  />
    <force:recordData aura:id="record"
                      layoutType="FULL"
                      recordId="{!v.recordId}"
                      targetError="{!v.recordError}"
                      targetRecord="{!v.record}"
                      targetFields ="{!v.simpleRecord}"
                      mode="EDIT"/>
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/> 
    
    <lightning:recordEditForm objectApiName="Campaign__c" density="comfy" recordId="{!v.recordId}"   onsuccess="{!c.onRecordSuccess}" >
        <lightning:messages /> 
        
        <div aura:id="editDialog" role="dialog" tabindex="-1" aria-labelledby="header43" class="slds-modal slds-fade-in-open">
            
            <div class="slds-modal__container">
                <div class="slds-modal__header">
                    <h2 class="slds-text-heading--medium"> 
                        <aura:if isTrue="{!v.recordId!=null}">
                            Edit {!v.record.fields.Name.value}
                            <aura:set attribute="else">
                                New {!v.Labelname} 
                            </aura:set>
                        </aura:if> 
                    </h2>
                </div>
                <div class="slds-modal__content slds-p-around--medium slds-grid slds-wrap ">
                    <div class="showScroll">
                        <ui:scrollerWrapper class="scrollerSizecart">
                            <lightning:layout multipleRows="true">  
                                <aura:iteration items="{!v.fieldSet}" var="field">
                                    <aura:if isTrue="{! (field=='Campaign_Description__c')}">
                                        <aura:set attribute="else"> 
                                            <lightning:layoutItem size="12" smallDeviceSize="12" mediumDeviceSize="12" largeDeviceSize="6" padding="around-small"> 
                                                <lightning:InputField  fieldName="{!field}"  class="{! 'slds-p-horizontal_x-small '+(field=='Name'?'frequired':'')}"   /> 
                                            </lightning:layoutItem>
                                        </aura:set>
                                    </aura:if>
                                    <!--aura:if isTrue="{!(field=='Start_Date__c' &amp;&amp; v.recordId==null)}">
                                        <lightning:layoutItem size="12" smallDeviceSize="12" mediumDeviceSize="12" largeDeviceSize="12" padding="around-small">
                                            <lightning:inputField fieldName="{!field}" aura:id="CmpStartDate" class=" slds-p-horizontal_x-small"  />
                                        </lightning:layoutItem>
                                    </aura:if--> 
                                    <aura:if isTrue="{!field=='Campaign_Description__c'}">
                                        <lightning:layoutItem size="12" smallDeviceSize="12" mediumDeviceSize="12" largeDeviceSize="12" padding="around-small">
                                            <lightning:inputField fieldName="{!field}" aura:id="CmpDesc" class=" slds-p-horizontal_x-small"  />
                                        </lightning:layoutItem>
                                    </aura:if> 
                                </aura:iteration>
                                <br></br>
                            </lightning:layout>
                        </ui:scrollerWrapper>
                    </div>
                </div>
                
                <div class="slds-modal__footer">                
                    <lightning:button variant="neutral" label="Cancel"  onclick="{!c.doCancel}"/>
                    <lightning:button  variant="brand"  label="Save" type="submit"  />
                </div>
            </div>
        </div>
    </lightning:recordEditForm> 
    <div aura:id="overlay" class="slds-backdrop slds-backdrop--open"></div>
    
    <aura:if isTrue="{!not(empty(v.recordError))}">
        <div class="recordError">
            {!v.recordError}</div>
    </aura:if>   
    
</aura:component>