public with sharing class ClientListViewSwitchExt
{
/*
    public boolean isLtng{private set; get{            String theme = UserInfo.getUITheme();                    return (theme=='Theme4d')?true:false;
                                            }
                }  
    
    public string Clientprefix{set;get;}

    public ClientListViewSwitchExt(ApexPages.StandardController controller) {

     Clientprefix=Client__c.SobjectType.getDescribe().getKeyPrefix();
    // ApexPages.addMessage(New Apexpages.Message(Apexpages.severity.info,''+glcodeprefix));
    }


public pageReference Clientview(){
   PageReference p;
   p =  new Pagereference('/'+Clientprefix+'/o');
    /* if(isLtng)
     {
             p =  new Pagereference('/'+Clientprefix+'/o');
     }
     else
     {
          p =  new Pagereference('/apex/ClientTabOverride');
     }
     p.setRedirect(true);
          return p;
  
}*/

}