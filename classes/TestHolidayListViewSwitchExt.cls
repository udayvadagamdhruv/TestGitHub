@isTest
public class TestHolidayListViewSwitchExt {

    @isTest static void testHolidaylistView(){
        
     /*   Holiday__c testH=new Holiday__c(Name='Test Holiday1',Date__c =System.Today());
        insert testH;
        HolidayListViewSwitchExt hllstviewc=new HolidayListViewSwitchExt(new Apexpages.StandardController(testH));
        hllstviewc.hollistview();
        */
        
    }
}