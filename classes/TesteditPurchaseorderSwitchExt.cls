@isTest
public class TesteditPurchaseorderSwitchExt {
	/* static TestMethod void TesteditPurchaseorderSwitchExt(){
        Job__c testJob1=new Job__c(Name='Job858',Start_Date__c=System.Today());
        insert testJob1;
        Estimate_Line_Items__c testPo=new Estimate_Line_Items__c(Name='Camp',Job__c=testJob1.id);
        insert testPo;
        editPurchaseorderSwitchExt controller = new editPurchaseorderSwitchExt(new ApexPages.StandardController(testPo));
      //  boolean x = controller.isMobile;
}*/
}