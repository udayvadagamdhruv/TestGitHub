@isTest
public class TestPOSwitchExt {
	/* static TestMethod void TestPOSwitchExt(){
        Job__c testJob1=new Job__c(Name='Job858',Start_Date__c=System.Today());
        insert testJob1;
        Purchase_Order__c testPo=new Purchase_Order__c(Name='Camp',Job__c=testJob1.id);
        insert testPo;
        POSwitchExt controller = new POSwitchExt(new ApexPages.StandardController(testPo));
      //  boolean x = controller.isMobile;
}*/ 
}