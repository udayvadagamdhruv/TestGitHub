<apex:page standardController="Document"  standardStylesheets="false" sidebar="false" showHeader="false"  contentType="html"   renderAs="pdf">
<!--html>
    <head> 
        <style type="text/css">
            div.tab
            {
                width:100%;
                border:solid black;
            }
            @page
            {
               /* Landscape orientation */
                size:landscape;

                /* Put page numbers in the bottom right corner of each
                    page in the pdf document. */
               @bottom-right 
               {
                  content: "Page " counter(page);
                  font-family: sans-serif;
                  font-size:8pt;
                }
                 @bottom-left 
                 {
                    content: "<apex:outputText value="{0,date,MMMM dd,yyyy}"><apex:param value="{!NOW()}" /></apex:outputText>";
                    background-repeat: no-repeat;
                    font-size:8pt;
                    font-family: sans-serif;
                }
             }
             body 
             {
                 font-family: sans-serif;
                 font-size:9pt;
             }

            th
            {
                 font-size:13pt
                 font-style:bold;
                 font-family: sans-serif;
            }
            td
            {
                font-size:11pt;
                font-family: sans-serif;
             }
  
             div.footer 
             {
                position : running(footer) ;
             }
        </style>
    </head>

    <body><!----{!sizeoflines } 
   
   
        <apex:image id="JS_logo" value="{!$Resource.JSCloudLogo}" rendered="{!imageNotFound == True}"/>
        <apex:image url="{!imageBody}" rendered="{!imageNotFound == False}"/>
        <div style="float:right;padding-right:35px;font-size:2.0em;font-style:normal;font-family: sans-serif;font-weight:bold">
        {!Document.Name}
        
        
        
        </div>
        <br/><br/>
    <apex:form >
    <apex:pageMessages id="pm"/>
    <apex:outputPanel >{!dipslaytable} </apex:outputPanel>
  
     <!--- <apex:repeat value="{!contentlines}" var="line"> 
       <apex:outputText value="{!line}"> <br/></apex:outputText> 
      </apex:repeat>-
      
    <div class="footer">
    
    
         <apex:outputLabel value="{!today}" style=" font-size:8pt;font-family: sans-serif;"></apex:outputLabel>
     </div>
    </apex:form> 
    </body>  
  </html-->
</apex:page>
<!-- End of Page -->