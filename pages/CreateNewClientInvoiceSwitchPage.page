<apex:page standardController="Client_Invoice__c" extensions="EditJobTaskSwitchPageExtension" docType="html-5.0" applyBodyTag="False">
  <apex:outputPanel rendered="{!if(isClassic,true,false)}">
        <apex:include pageName="CreateNewClientInvoice"/>
    </apex:outputPanel> 
    <!--apex:outputPanel rendered="{!if(isMobile,true,false)}">
         <apex:include pageName="EditJobTaskMobile"/>
    </apex:outputPanel>
    -->
    <apex:outputPanel rendered="{!if(isLight,true,false)}">
         <apex:include pageName="CreateNewClientInvoiceLightning"/>
    </apex:outputPanel>
</apex:page>