<!-- This page is used to display All Clients Job for logged in Client Contact. --> 
<apex:page sidebar="false" showHeader="true" standardController="Job__c" tabStyle="Client_Job__tab">
   <!--apex:form >
      <apex:pageBlock >
       
         <apex:selectList multiselect="false" size="1" id="listname" value="{!selectedItem}">
            <apex:selectOptions value="{!items}" id="pb"> 
               <apex:actionsupport reRender="pblock" action="{!filterJobs}" event="onchange"></apex:actionSupport>
            </apex:selectOptions>
         </apex:selectList>
     
         &nbsp;&nbsp;
         <apex:commandButton action="{!filterJobs}" value="Go"/> 
         <br/><br/>
   
         <apex:outputPanel layout="block" rendered="{!lstJob1.size>0}" style="overflow:auto;width:100%;height:300px;">
         
            <apex:pageBlockTable id="pblock1" value="{!lstJob1}" var="lst" rendered="{!lstJob1.size > 0}" columns="6" style="width:100%">
        
               <apex:column headerValue="{!$ObjectType.Job__c.Fields.Name.Label}" width="30%">
               
                  <apex:outputLink value="/apex/JobListView?id={!lst.id}" target="_top">
                      {!lst.Name}
                  </apex:outputLink>
               
               </apex:column> 
        
               <apex:column value="{!lst.Job_Auto_Number__c}" headerValue="{!$ObjectType.Job__c.Fields.Job_Auto_Number__c.Label}" width="10%"></apex:column>
               
               <apex:column value="{!lst.Start_Date__c}" headerValue="{!$ObjectType.Job__c.Fields.Start_Date__c.Label}" width="10%"></apex:column>
               
               <apex:column value="{!lst.Due_Date__c}" headerValue="{!$ObjectType.Job__c.Fields.Due_Date__c.Label}" width="10%"></apex:column>
               
               <apex:column value="{!lst.Status__c}" headerValue="{!$ObjectType.Job__c.Fields.Status__c.Label}" width="10%"></apex:column>
               
               <apex:column value="{!lst.Schedule_Template__r.Name}" headerValue="{!$ObjectType.Job__c.Fields.Schedule_Template__c.Label}" width="10%"></apex:column>
            
            </apex:pageBlockTable>
        
         </apex:outputPanel>
 
       
         <apex:pageBlockSection rendered="{!lstJob1.size <= 0}">
          
            <apex:pageBlockSectionItem >
               <b>No records to display</b>
            </apex:pageBlockSectionItem>              
         
         </apex:pageBlockSection>
      
      </apex:pageBlock>
   </apex:form-->
</apex:page>
<!-- End of Page -->